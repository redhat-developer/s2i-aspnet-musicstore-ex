#!/bin/bash
source /opt/app-root/etc/generate_container_user

set -e

CWD="samples/MusicStore.Standalone"

echo "---> Running musicstore demo ..."
[ -n "$CWD" ] && [ -d "$CWD" ] && (cd $CWD && dotnet run)
